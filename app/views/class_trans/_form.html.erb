<%= form_with(model: class_tran) do |form| %>
  <% if class_tran.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(class_tran.errors.count, "error") %> prohibited this class_tran from being saved:</h2>

      <ul>
        <% class_tran.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
